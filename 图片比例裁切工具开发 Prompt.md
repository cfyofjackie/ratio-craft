请帮我开发一个极简的图片比例裁切工具，第一版优先做成纯前端静态网页，不需要后端，可以直接部署到 GitHub Pages。后续我可能会把它迁移到小红书小程序，所以请尽量把“图片处理核心逻辑”和“网页 UI”分离，方便复用。

## 核心功能

用户操作流程：

1. 上传 / 选择一张本地图片。
2. 选择目标比例：
   - 1:1
   - 3:4
   - 4:3
   - 2:3
   - 3:2
   - 9:16
   - 16:9
   - 自定义比例，例如输入 6:9。
3. 选择比例后，显示一个固定比例的裁切区域。
4. 用户可以：
   - 鼠标拖动图片调整位置；
   - 鼠标滚轮缩放；
   - 手机端通过单指拖动、双指缩放；
   - 图片始终覆盖整个裁切区域，不能露出空白。
5. 实时显示：
   - 原始图片分辨率；
   - 当前裁切区域对应的最终输出分辨率。
6. 支持一键恢复默认位置和缩放。
7. 支持旋转图片 90°。
8. 支持导出：
   - JPG
   - PNG

## 最重要的画质要求

这个工具不能主动降低用户图片的画质。

页面中的预览图可以为了性能缩小显示，但最终导出时绝对不能直接截取低分辨率预览 Canvas。

必须：

- 始终保留原始图片数据；
- 将屏幕上的裁切框、位移、缩放比例映射回原始图片像素坐标；
- 最终从原始分辨率图片中计算并裁切；
- 默认不主动 resize；
- 不进行不必要的二次降采样；
- 不自动把图片压缩到 1080p、2K 等固定尺寸；
- 如果原图为 6000×4000，裁切后能保留多少原始像素就保留多少；
- JPG 导出默认使用高质量设置，例如 quality 0.98；
- PNG 使用无损导出；
- 不允许因为页面显示尺寸较小而导致最终导出的图片也只有页面预览分辨率。

核心原则：

**Preview resolution ≠ Export resolution**

以及：

**Never downscale unless the user explicitly requests it.**

第一版暂时不需要提供“修改输出尺寸”功能，只需要忠实裁切原图。

## UI 风格

整体尽量简洁、现代、轻量，不要做成 Photoshop 那种复杂图片编辑器。

建议布局：

顶部：
- 工具名称
- 选择图片

中间：
- 大面积图片裁切预览区
- 裁切框固定，图片在裁切框内部移动和缩放

底部工具栏：

比例：
`1:1 | 3:4 | 4:3 | 2:3 | 3:2 | 9:16 | 16:9 | 自定义`

其他操作：
`旋转 | 重置`

信息：
`原图：6000 × 4000`
`输出：3000 × 4000`

导出：
`JPG | PNG | 导出图片`

UI 以浅色、干净、留白充足为主，桌面端和手机端都要正常使用。

## 技术要求

优先使用：

- HTML
- CSS
- Vanilla JavaScript

如果没有必要，不要引入大型框架。

图片读取可以使用：
- `URL.createObjectURL()`
- 或 File API

裁切 / 导出可以使用 Canvas API，但请注意导出 Canvas 必须按照最终原图像素尺寸创建，而不是按照屏幕预览尺寸创建。

交互优先使用 Pointer Events，让鼠标和触摸尽可能共用一套逻辑。

手机端需要支持：
- 单指拖动
- 双指 pinch zoom

## 代码结构

请不要把所有代码全部塞进一个巨大 HTML 文件。

建议类似：

```text
/
├─ index.html
├─ css/
│  └─ style.css
├─ js/
│  ├─ app.js
│  ├─ crop-engine.js
│  └─ image-utils.js
└─ README.md
```

其中：

`crop-engine.js`

只负责：
- 图片位置
- 缩放
- 比例
- 裁切区域
- 原图坐标换算
- 导出所需要的 crop 参数

尽量不要和 DOM 强耦合。

这样以后迁移到小红书小程序时，可以最大程度复用核心算法。

## 隐私要求

所有图片处理必须完全在用户本地浏览器完成。

不要：
- 上传图片到服务器；
- 调用第三方图片 API；
- 存储用户图片。

页面可以明确提示：

“图片仅在本地处理，不会上传服务器。”

## 第一版开发优先级

请先确保以下核心功能真的可用：

1. 选择图片
2. 比例切换
3. 拖动
4. 缩放
5. 手机双指缩放
6. 原图像素坐标映射
7. 高质量原图裁切
8. JPG / PNG 导出
9. 响应式布局

不要一开始增加滤镜、文字、水印、AI 扩图等功能。

这个项目的目标不是做完整图片编辑器，而是做一个：

**极快、极简、不损失额外画质的图片比例裁切工具。**

完成后，请自行检查至少以下情况：

- 横图裁成 3:4；
- 竖图裁成 4:3；
- 超高分辨率图片；
- 图片大幅放大后裁切；
- JPG 导出分辨率是否正确；
- PNG 导出分辨率是否正确；
- 手机触摸操作是否正常；
- 导出图片是否和用户看到的裁切区域一致。

## 实现决策（已确认）

以下是实现前已经确定的交互与边界行为，写代码时以本节为准。

### 1. 切换比例时重置图片位置和缩放

切换比例（包括点预设比例和提交自定义比例）后，图片回到"默认铺满"状态：刚好覆盖整个裁切框、居中、最小缩放。不保留之前调好的位移和缩放，因为裁切框形状变了，旧构图大概率失效，保留旧状态反而让用户困惑。用户重新拖动即可。

### 2. 旋转 90° 只转图片，不转裁切框、不改变输出尺寸

点"旋转"后，画面上是图片内容转了 90°，裁切框保持不动，导出尺寸仍由所选比例决定。不做"旋转后输出也跟着变竖"的方案，否则等于偷偷改了用户选的比例。

实现要求：旋转必须进入原图坐标换算的数学里（即 crop-engine 中的状态模型），不允许只用 CSS 旋转预览层然后照常导出。"图片坐标系 ↔ 原图坐标系"在旋转 0° / 90° / 180° / 270° 下的映射关系要先在纸上推导清楚再动手，这是 crop-engine 最容易出 bug 的部分。

### 3. 超大图不做限制，但必须有导出失败兜底

第一版不做自动缩小（见画质红线），也不限制上传图片的分辨率。但移动端浏览器对单个 Canvas 的总像素数有上限（iOS Safari 大约 1600 万像素级别），超出后导出会静默失败。

要求：导出前计算目标输出尺寸，若超出安全上限（建议按 1600 万像素做常量，并留注释说明依据），给出明确的失败提示（例如"这张图太大，当前浏览器无法直接导出"），不允许出现"点了导出没反应且无任何提示"的情况。大图分块导出等真支持方案留到后续版本。

### 4. 解码时应用 EXIF 方向

手机竖拍照片实际存储为横向原始数据 + EXIF Orientation 标签。浏览器显示 `<img>` 时会自动转正，但直接把图片画到 Canvas 上不处理该标签会导致导出结果横竖颠倒，且普通网图测不出来，必须用真手机照片验证。

要求：图片解码阶段统一要求"应用 EXIF 方向"（如 `createImageBitmap` 的 `imageOrientation: 'from-image'`，或等效方案），保证全链路（预览、坐标换算、导出）基于同一个已转正的图片尺寸。这属于一开始就要做对的基础项，不是后期补丁。

### 5. 透明图导出 JPG 时填白色底

JPG 不支持透明通道，浏览器默认把透明区域填成黑色。导出 JPG 前先在导出 Canvas 上铺白色底再绘制图片；PNG 导出保持透明不变。

### 6. 自定义比例的输入规则

- 格式：`数字:数字`，允许小数（如 `2.35:1`）；
- 校验：0、负数、缺冒号、非数字一律拒绝提交，输入框给出可见的错误状态；
- 化简：合法输入按最简整数比化简（如 `6:9` → `2:3`），并在信息栏显示实际生效的比例，不改动用户输入框里的内容；
- 生效时机：回车或失焦即生效，裁切框立即更新。

### 7. 精准预览缩放输入框（绝对倍数语义）

提供倍数输入框（如输入 `2` 表示放大两倍、`1.1` 放大 1.1 倍、`0.5` 缩小一半）：

- 语义：**绝对倍数，相对于初始铺满状态（cover，即信息栏的 1.00×）**。输入 3 就是初始大小的 3 倍，无论当前处于几倍——不基于当前状态相乘，不需要先点重置。信息栏显示的倍数永远等于最近一次输入生效的值；
- 锚点：以裁切框中心为锚点缩放（数字输入的可预期行为，与手势缩放以光标/双指为锚点不同）；
- 上限：仍受"最大 16 倍 cover 缩放"钳制、下限为 cover（缩小到铺满为止），超出时钳制到边界并提示；
- 校验：正数（允许小数），0、负数、非数字拒绝并提示；
- 生效时机：回车或失焦即生效；
- 信息栏实时显示当前缩放倍数（相对 cover 状态，cover 时为 1.00×）；
- 该功能只改变预览构图，导出仍按原图像素换算，画质承诺不变。

### 8. 批量上传与侧边栏

支持一次选择/拖拽/粘贴多张图片，显示在侧边栏（手机端为顶部横向滑动的缩略图条）。点缩略图切换当前编辑的图片，无需反复上传。

- **每张图各自记忆全部编辑状态**：比例、缩放、位置、旋转。切走再切回，原样恢复；
- **比例跟随每张图**（见决策 10）：A 图 3:4、B 图 4:3，各自的裁切框各自适配；
- 手动选择的比例只影响当前这张图；
- 缩略图上标注该图的比例角标；已导出过的图片标记小勾，方便核对进度；缩略图可删除；
- **内存策略**：文件对象（File）全部保留在内存（代价小），但解码后的图像数据只保留当前正在编辑的一张，切换时重新解码。避免几十张高分辨率照片撑爆浏览器内存。代价是切换大图时有零点几秒加载感，切换时给出加载提示。

### 9. 全部导出与指定保存位置

- **全部导出**：一键把侧边栏每张图按各自记住的状态（比例/缩放/位置/旋转）依次自动导出。浏览器可能弹一次"允许下载多个文件"的确认，属正常现象；
- **指定保存位置**：提供"选择保存文件夹"按钮。支持的浏览器（Chrome/Edge 桌面端）通过 File System Access API 弹一次系统文件夹选择框，授权后导出（单个和全部）直接写入该文件夹；授权记住（localStorage），下次免选。不支持的浏览器（Safari/Firefox/移动端）隐藏该按钮，回退为默认下载行为（用户可在浏览器设置中开启"每次下载前询问保存位置"）；
- 导出文件名格式：`原文件名_比例_宽x高.扩展名`（如 `photo_3x4_1500x2000.jpg`），避免批量导出重名。

### 10. 比例自动识别并自动应用（比例跟随每张图）

上传或切入一张图片时，按其（EXIF 转正后的）宽高自动检测比例并**直接应用**为该图的裁切比例，信息栏提示（如"已自动应用原图比例 3:2"）。

- **逐张自动适配**：批量图片中点开哪张，裁切框就自动适配哪张的比例；
- **手动覆盖永久优先**：一旦用户对该图手动选择了比例，该图记住手动选择，之后再切入不再自动跳回原图比例。"自动适配"只对未手动改过比例的图生效；
- **丑比例归拢规则**：先将宽高比与常见预设（1:1、4:3、3:4、3:2、2:3、16:9、9:16）比对，相对误差小于 1% 即归入该预设（如 4030×3026 归为 4:3）；真实偏离超过 1% 的（如全景 2.35:1）按精确化简比例照实显示并裁切。检测基于 EXIF 转正后的方向，竖拍照片正确显示 3:4 而非 4:3。

### 11. 井字格辅助线

裁切框内叠加三等分辅助线（两条竖线、两条横线），辅助构图。

- 纯预览层叠加，**绝不进入导出图像**；
- 工具栏提供"井字格"开关按钮；
- 默认开启，用户的开关偏好存 localStorage，下次打开保持。

### 12. 预览尺寸变化时保持构图

浏览器窗口缩放、侧边栏出现等导致预览区尺寸变化时，用户已选定的构图必须保持不动：

- 保留**相对缩放倍数**（相对 cover）；
- 保留**裁切框中心对应的图像内容点**：重算布局时将该点重新对准新框中心。两者共同保证新框内画面是旧画面的等比缩放版，视觉上构图纹丝不动；
- 边界钳制（图片必须盖满裁切框）仍然生效，钳制时按"尽量少动"原则贴近原位置；
- 导出结果不受预览尺寸影响：输出分辨率 = 框尺寸 ÷ 缩放倍数，两者同比例变化，商不变。

最后请在 README 中说明：

- 如何本地运行；
- 如何部署 GitHub Pages；
- 当前功能；
- 后续如果迁移到小红书小程序，哪些模块可以直接复用。
## 拼图模式（二期功能，设计已确认，待实现）

### 定位与工作流

拼图消费的是**裁切后的成品**，不是原始照片。典型工作流：裁切模式批量调好 N 张图 → 点"拼图" → 从已裁切列表中选出需要的几张 → 自动组成拼图 → 导出。拼图模式内**不再有每格裁切编辑器**（那是旧方案，已废弃）——图进来之前构图已经完成。裁切成品由"已保存状态 + 原图"实时渲染，不需要先导出成文件再拼。

### 入口

裁切模式工具栏提供"拼图"按钮，点击进入拼图视图；提供"返回裁切"回到原模式。两个模式共享侧边栏图片库。

### 模板（格数固定）

- 上下两张（2 格）、左右两张（2 格）、田字格（4 格）、九宫格（9 格）；
- 每个模板的格数固定，必须放正好对应数量的图片。

### 画布比例

- **上下 / 左右**：每格保持自己照片裁切成品的原始比例（专属行为）。上下模式所有格共宽（1080），高度按各自比例计算；左右模式共高。画布 = 各格之和 → **零裁切、零留白**（两张 4:3 上下排 → 整图 2:3）；
- **田字 / 九宫**：格子必须等大（网格约束），画布比例由用户选择，提供成品比例列表：**自适应（默认选项之一）、1:1、3:4（默认选中）、4:3、9:16、16:9**；
  - 选明确比例：画布严格按该比例被九格等分（每格同形状），照片 cover 铺满、无留白，比例不符的照片轻微裁边（网格等分的数学必然，接受）；
  - 选自适应：画布比例取所有选中照片裁切比例的算术平均值（9 张同比例时零裁切；未选图时按 3:4 显示）。

### 填充规则

- 所有格子一律 **cover 铺满**：无留白、无背景色填充；
- 照片分辨率不足以撑满格子时直接放大（插值），不做限制（已确认：优先无留白，接受放大）。

### 每格尺寸

- 每格长边固定 1080px（上下模式：公共宽 1080；左右模式：公共高 1080；田/九宫：每格长边 1080）；
- 整图输出 = 各格之和 + 间距，导出前在界面显示总输出尺寸。

### 选图与位置（自由位置）

- **点击切换**：点图库中的图加入拼图（按顺序入格）；**再点同一条已选的图 = 取消它**（后方图片顺序前移补位）。存在激活格子时点图为"放入/替换该格"；
- **所见即所得拖拽**：图片从侧边栏直接拖进任意格子（目标格高亮，松手放入/替换）；格子之间按住拖动互换位置（拖动中源格半透明、落点格高亮）；
- 空格显示"＋"占位，放满才能导出。

### 界面

- 拼图预览**一律铺满预览区**（预览只是显示，允许放大，不影响导出画质）；
- 侧边栏撑满视口高度，缩略图列表占据剩余空间、大图显示，图片多时整列滚动。

### 导出

- 整图导出 JPG（quality 0.98）/ PNG；复用"选择保存文件夹"与导出兜底（画布上限检查）；
- 文件名形如 `collage_3x3_3260x3260.jpg`；
- 格间距：滑杆 0–40px，默认 0；格与格之间的缝隙不视为"留白"（留白特指格子内部未铺满）。
